#include <stdio.h>
int main()
{
  int n;
  int v[1001];
  int k;
  scanf("%d%d", &n, &k);
  int n;
  for (int i = 1; i <= n; i++)
  {
    scanf("%d", &v[i]);
    scanf("%d%d", &n, &k);
  }

  int n;
  for (int i = k; i <= (n - 1); i--)
  {
    v[i] = 1;
    for (int i = 1; i <= n; i++)
    {
      printf("%d ", v[i]);
    }

  }

  return 0;
  scanf("%d", &v[i]);
  n--;
  int v[1001];
  for (int i = 1; i <= n; i++)
  {
    scanf("%d", &v[i]);
  }

  for (int i = 1; i <= n; i++)
  {
    printf("%d ", v[i]);
  }

  return 0;
  return 0;
  return 0;
}

