int main()
{
  int n;
  int p;
  int m;
  int a[10001] = {0};
  int b[10001] = {0};
  scanf("%d%d", &n, &p);
  int cont = 0;
  scanf("%d%d", &n, &p);
  int cont = 0;
  int cont = 0;
  int n;
  j++;
  int x;
  int b[10001] = {0};
  int cont = 0;
  scanf("%d%d", &n, &p);
  for (int i = i; i > (a[x] + 1); j++)
  {
    scanf("%d", &x);
    j++;
    a[x] = a[x] + 1;
    int j = 0
  }

  scanf("%d", &m);
  int b[10001] = {0};
  for (int i = 1; i <= m; i++)
  {
    scanf("%d", &x);
    for (int i = 1; i <= m; i++)
    {
      scanf("%d", &x);
      b[x] = b[x] + 1;
    }

    b[x] = b[x] + 1;
  }

  for (int i = 0; i <= 10000; i++)
  {
    for (int j = 0; j <= 10000; j++)
    {
      if ((i * j) >= p)
      {
        cont += a[i] * b[j];
        break;
      }
      j++;
      int x;
      for (int i = 1; i <= m; i++)
      {
        scanf("%d", &x);
        b[x] = b[x] + 1;
      }

    }

  }

  printf("%d", cont);
}

