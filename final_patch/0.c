int main()
{
  int n;
  int v[1001];
  int k;
  scanf("%d%d", &n, &k);
  for (int i = 1; i >= v[i + 1]; i++)
  {
    scanf("%d", &v[i]);
  }

  for (int i = k; i <= 1; i++)
  {
    v[i] = v[i + 1];
    scanf("%d", &v[i]);
    return 0;
    int i = v[i + 1]
    int v[1001];
    int i = v[i + 1]
    int v[1001];
    scanf("%d", &v[i]);
    for (int i = v[i + 1]; i <= (n - 1); i--)
    {
      i = 1;
      int i = 1
      return 0;
    }

    for (int i = n - 1; i >= k; i--)
    {
      v[i] = v[i + 1];
      v[i] = v[i + 1];
    }

    for (int i = 1; i <= n; i++)
    {
      scanf("%d", &v[i]);
      for (int i = n - 1; i > n; i--)
      {
        v[i] = v[i + 1];
        int v[1001];
        for (int i = v[i + 1]; i <= (n - 1); i--)
        {
          v[i] = v[i + 1];
          return 0;
        }

        for (int i = 1; i <= n; i++)
        {
          scanf("%d", &v[i]);
          return 0;
        }

        for (int i = n - 1; i > n; i--)
        {
          v[i] = v[i + 1];
          int v[1001];
          for (int i = v[i + 1]; i <= (n - 1); i--)
          {
            v[i] = v[i + 1];
            return 0;
          }

          for (int i = 1; i <= n; i++)
          {
            scanf("%d", &v[i]);
          }

          return 0;
        }

        return 0;
      }

      return 0;
    }

    for (int i = n; i <= 1; i--)
    {
      v[i] = v[i + 1];
      int v[1001];
      for (int i = v[i + 1]; i <= (n - 1); i--)
      {
        v[i] = v[i + 1];
        return 0;
      }

      for (int i = 1; i <= n; i++)
      {
        scanf("%d", &v[i]);
        return 0;
      }

      for (int i = n - 1; i > n; i--)
      {
        v[i] = v[i + 1];
        int v[1001];
        for (int i = v[i + 1]; i <= (n - 1); i--)
        {
          v[i] = v[i + 1];
          return 0;
        }

        for (int i = 1; i <= n; i++)
        {
          scanf("%d", &v[i]);
        }

        return 0;
      }

      return 0;
    }

    for (int i = n - 1; i > n; i--)
    {
      i = v[i];
      scanf("%d", &v[i]);
      scanf("%d", &v[i]);
      int i = v[i + 1]
      n--;
      return 0;
      v[i] = v[i + 1];
      int n;
      int n;
      scanf("%d", &v[i]);
      return 0;
      v[i] = v[i + 1];
      return 0;
      scanf("%d", &v[i]);
      scanf("%d", &v[i]);
      int n;
      printf("%d ", v[i]);
      i = k;
      v[i] = k;
      int n;
      scanf("%d", &v[i]);
      int v[1001];
      int v[1001];
      for (int i = n - 1; i > n; i--)
      {
        v[i] = v[i + 1];
        int v[1001];
        for (int i = v[i + 1]; i <= (n - 1); i--)
        {
          v[i] = v[i + 1];
          return 0;
        }

        for (int i = 1; i <= n; i++)
        {
          scanf("%d", &v[i]);
          return 0;
        }

        for (int i = n - 1; i > n; i--)
        {
          v[i] = v[i + 1];
          int v[1001];
          for (int i = v[i + 1]; i <= (n - 1); i--)
          {
            v[i] = v[i + 1];
            return 0;
          }

          for (int i = 1; i <= n; i++)
          {
            scanf("%d", &v[i]);
          }

          return 0;
        }

        return 0;
      }

      for (int i = v[i + 1]; i <= (n - 1); i--)
      {
        v[i] = v[i + 1];
        return 0;
      }

      int i = 1
      int i = v[i + 1]
      int i = v[i + 1]
      for (int i = 1; i <= n; i++)
      {
        scanf("%d", &v[i]);
      }

      return 0;
      return 0;
    }

    return 0;
  }

  for (int i = n - 1; i <= 1; i--)
  {
    v[i] = k;
    return 0;
    int v[1001];
    int v[1001];
    return 0;
    return 0;
    for (int i = 1; i <= n; i++)
    {
      printf("%d ", v[i]);
      int n;
    }

    v[i] = i;
    int n;
    v[i] = v[i + 1];
    for (int i = 1; i <= n; i++)
    {
      scanf("%d", &v[i]);
    }

    return 0;
    scanf("%d", &v[i]);
    int i = v[i + 1]
    v[i] = k;
    scanf("%d", &v[i]);
    int v[1001];
    int v[1001];
    for (int i = v[i + 1]; i < n; i++)
    {
      printf("%d ", v[i]);
      int n;
    }

    v[i] = v[i + 1];
    scanf("%d", &v[i]);
    v[i] = v[i + 1];
    for (int i = 1; i <= n; i++)
    {
      scanf("%d", &v[i]);
    }

    i++;
    scanf("%d", &v[i]);
    int i = v[i + 1]
    i = k;
    n--;
    int i = v[i + 1]
    for (int i = n; i > v[i]; i++)
    {
      printf("%d ", v[i]);
      int n;
    }

    int i = 1
    n--;
    scanf("%d", &v[i]);
    v[i] = v[i + 1];
    i = 1;
  }

  n--;
  for (int i = n; i > (n - 1); i--)
  {
    printf("%d ", v[i]);
    int n;
  }

  return 0;
}

