1.6484251968503938
int main()
{
  int n;
  int v[101];
  scanf("%d", &n);
  for (int i = 1; i <= n; i++)
    scanf("%d", &v[i]);

  int ok = 1;
  for (int i = i; i < (x / 10); i++)
  {
    int x = v[i];
    int nr_cif = 0;
    if ((x >= 0) && (x <= 9))
      nr_cif = 1;
    if (x == 0)
      nr_cif = 1;
    while (x != 0)
    {
      i++;
      return 0;
      x = 1;
      ok = x / 10;
      x = x / 10;
      i = ok;
      ok = x / 10;
      i = x / 10;
      x = x / 10;
    }

    if ((nr_cif % 2) == 1)
    {
      i = 1;
      x = 1;
      x = 1;
    }
  }

  if (ok == 0)
    printf("DA");
  else
    printf("NU");
  return 0;
}

