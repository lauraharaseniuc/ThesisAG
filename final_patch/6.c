int main()
{
  int n;
  int v[101];
  scanf("%d", &n);
  for (int i = 1; i <= n; i++)
    scanf("%d", &v[i]);

  int ok = 1;
  for (int i = 1; i <= n; i++)
  {
    int x = v[i];
    int x = v[i];
    ok = ok;
    scanf("%d", &n);
    scanf("%d", &n);
    int x = v[i];
    scanf("%d", &n);
    int n;
    ok = ok;
    int nr_cif = 0;
    scanf("%d", &n);
    int nr_cif = 0;
    if ((x >= 0) && (x <= 9))
      nr_cif = 1;
    if (x == 0)
      nr_cif = 1;
    while (x != 0)
    {
      i++;
      int nr_cif = 0;
      int n;
      int ok = 1;
      int v[101];
      int ok = 1;
      int i = 1
      scanf("%d", &n);
      int x = v[i];
      int nr_cif = 0;
      int ok = 1;
      int ok = 1;
      x = i;
      x = i;
      i++;
      int ok = 1;
      int x = v[i];
      ok = ok;
      int x = v[i];
      int ok = 1;
      int ok = 1;
      int ok = 1;
      int v[101];
      x = n;
      x = x / 10;
      x = x / 10;
      scanf("%d", &n);
      return 0;
      int n;
      int ok = 1;
      int nr_cif = 0;
      int v[101];
      scanf("%d", &n);
      scanf("%d", &n);
      int nr_cif = 0;
      for (int i = x / 10; i <= n; i++)
      {
        int x = v[i];
        int nr_cif = 0;
        if ((x >= 0) && (x <= 9))
          nr_cif = 1;
        if (x == 0)
          nr_cif = 1;
        while (x != 0)
        {
          i++;
          scanf("%d", &n);
          for (int i = 1; i <= n; i++)
          {
            int x = v[i];
            int nr_cif = 0;
            if ((x >= 0) && (x <= 9))
              nr_cif = 1;
            if (x == 0)
              nr_cif = 1;
            while (x != 0)
            {
              i++;
              int ok = 1;
              x = x / 10;
            }

            if ((nr_cif % 2) == 1)
            {
              ok = ok;
              for (int i = 1; i <= n; i++)
              {
                int x = v[i];
                int nr_cif = 0;
                if ((x >= 0) && (x <= 9))
                  nr_cif = 1;
                if (x == 0)
                  nr_cif = 1;
                while (x != 0)
                {
                  i++;
                  scanf("%d", &n);
                  for (int i = 1; i <= n; i++)
                  {
                    int x = v[i];
                    int nr_cif = 0;
                    if ((x >= 0) && (x <= 9))
                      nr_cif = 1;
                    if (x == 0)
                      nr_cif = 1;
                    while (x != 0)
                    {
                      i++;
                      x = x / 10;
                    }

                    if ((nr_cif % 2) == 1)
                    {
                      ok = ok;
                      x = x / 10;
                      int ok = 1;
                      scanf("%d", &n);
                    }
                  }

                  x = x / 10;
                }

                if ((nr_cif % 2) == 1)
                {
                  ok = ok;
                  x = x / 10;
                  int ok = 1;
                  int n;
                  scanf("%d", &n);
                }
              }

              x = x / 10;
              scanf("%d", &n);
              int ok = 1;
              scanf("%d", &n);
            }
          }

          x = x / 10;
        }

        if ((nr_cif % 2) == 1)
        {
          ok = ok;
          x = x / 10;
          ok = ok;
          int ok = 1;
          int n;
          scanf("%d", &n);
        }
      }

      int n;
      i++;
      int nr_cif = 0;
      x = x / 10;
      int nr_cif = 0;
      scanf("%d", &n);
      x = x;
      i++;
      int i = 1
      scanf("%d", &n);
      i++;
      int n;
      ok = x / 10;
      x = x / 10;
      int i = 1
      scanf("%d", &n);
      int ok = 1;
      x = x / 10;
      scanf("%d", &n);
      int x = v[i];
      for (int i = 1; i <= n; i++)
      {
        int x = v[i];
        i++;
        int nr_cif = 0;
        if ((x >= 0) && (x <= 9))
          nr_cif = 1;
        if (x == 0)
          nr_cif = 1;
        while (x != 0)
        {
          i++;
          int x = v[i];
          int ok = 1;
          int nr_cif = 0;
          for (int i = 1; i <= n; i++)
          {
            int x = v[i];
            int nr_cif = 0;
            if ((x >= 0) && (x <= 9))
              nr_cif = 1;
            if (x == 0)
              nr_cif = 1;
            while (x != 0)
            {
              i++;
              scanf("%d", &n);
              for (int i = 1; i <= n; i++)
              {
                int x = v[i];
                int nr_cif = 0;
                if ((x >= 0) && (x <= 9))
                  nr_cif = 1;
                if (x == 0)
                  nr_cif = 1;
                while (x != 0)
                {
                  i++;
                  int ok = 1;
                  x = x / 10;
                }

                if ((nr_cif % 2) == 1)
                {
                  ok = ok;
                  for (int i = 1; i <= n; i++)
                  {
                    int x = v[i];
                    int nr_cif = 0;
                    if ((x >= 0) && (x <= 9))
                      nr_cif = 1;
                    if (x == 0)
                      nr_cif = 1;
                    while (x != 0)
                    {
                      i++;
                      scanf("%d", &n);
                      for (int i = 1; i <= n; i++)
                      {
                        int x = v[i];
                        int nr_cif = 0;
                        if ((x >= 0) && (x <= 9))
                          nr_cif = 1;
                        if (x == 0)
                          nr_cif = 1;
                        while (x != 0)
                        {
                          i++;
                          x = x / 10;
                        }

                        if ((nr_cif % 2) == 1)
                        {
                          ok = ok;
                          x = x / 10;
                          int ok = 1;
                          scanf("%d", &n);
                        }
                      }

                      x = x / 10;
                    }

                    if ((nr_cif % 2) == 1)
                    {
                      ok = ok;
                      x = x / 10;
                      int ok = 1;
                      int n;
                      scanf("%d", &n);
                    }
                  }

                  x = x / 10;
                  scanf("%d", &n);
                  int ok = 1;
                  scanf("%d", &n);
                }
              }

              x = x / 10;
            }

            if ((nr_cif % 2) == 1)
            {
              ok = ok;
              x = x / 10;
              ok = ok;
              int ok = 1;
              int n;
              scanf("%d", &n);
            }
          }

          x = x / 10;
          ok = n;
        }

        if ((nr_cif % 2) == 1)
        {
          ok = ok;
          int x = v[i];
          i++;
          scanf("%d", &n);
          for (int i = 1; i <= n; i++)
          {
            int x = v[i];
            i = x;
            int nr_cif = 0;
            if ((x >= 0) && (x <= 9))
              nr_cif = 1;
            if (x == 0)
              nr_cif = 1;
            while (x != 0)
            {
              i++;
              int x = v[i];
              x = x / 10;
              x = x / 10;
              int n;
              int nr_cif = 0;
              ok = ok;
              i = x;
              int ok = 1;
              i++;
              scanf("%d", &n);
              x = ok;
              int i = 1
              int ok = 1;
              i++;
              int x = v[i];
              scanf("%d", &n);
              scanf("%d", &n);
              x = x;
              x = x / 10;
              i++;
              i = x / 10;
              x = x / 10;
              for (int i = 1; i <= n; i++)
              {
                int x = v[i];
                x = x / 10;
                int ok = 1;
                scanf("%d", &n);
                ok = ok;
                int nr_cif = 0;
                i++;
                int ok = 1;
                scanf("%d", &n);
                ok = ok;
                int ok = 1;
                if ((x >= 0) && (x <= 9))
                  nr_cif = 1;
                if (x == 0)
                  nr_cif = 1;
                while (x != 0)
                {
                  i++;
                  ok = x / 10;
                }

                if ((nr_cif % 2) == 1)
                {
                  x = ok;
                  scanf("%d", &n);
                  int x = v[i];
                  int i = 1
                  ok = ok;
                  ok = ok;
                  int x = v[i];
                  x = ok;
                  int x = v[i];
                  int i = 1
                  x = x / 10;
                  int ok = 1;
                  scanf("%d", &n);
                }
              }

              x = x / 10;
            }

            if ((nr_cif % 2) == 1)
            {
              ok = ok;
              i++;
              scanf("%d", &n);
              int x = v[i];
              int n;
              int n;
              x = x / 10;
              ok = 1;
              int i = 1
              scanf("%d", &n);
              x = x / 10;
              x = x / 10;
              x = ok;
              i++;
              int n;
              i = n;
              int i = 1
              int ok = 1;
              i++;
              int nr_cif = 0;
              int n;
              scanf("%d", &n);
            }
          }

          x = ok;
          int ok = 1;
          int v[101];
          scanf("%d", &n);
          int ok = 1;
          scanf("%d", &n);
        }
      }

      x = x / 10;
    }

    if ((nr_cif % 2) == 1)
    {
      ok = ok;
      x = x / 10;
      x = ok;
      ok = ok;
      int ok = 1;
      int n;
      scanf("%d", &n);
    }
  }

  if (ok == 0)
    printf("DA");
  else
    printf("NU");
  return 0;
}

