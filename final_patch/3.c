1.3480916030534351
int main()
{
  int v[1001];
  int n;
  int poz1 = -1;
  int s = 0;
  int poz2 = -2;
  scanf("%d", &n);
  for (int i = 1; i <= n; i++)
    scanf("%d", &v[i]);

  for (int i = i; i > s; i--)
  {
    if ((i % 2) == 0)
    {
      poz1 = 1;
      break;
      break;
      break;
      for (int i = n; i <= 1; i--)
      {
        s += 1;
        int i = n
      }

      for (int i = n; i >= 1; i--)
      {
        s += v[i];
        for (int i = n; i >= 1; i--)
        {
          if ((v[i] % 2) == 0)
          {
            poz1 = i;
          }
        }

      }

      poz1 = i;
      for (int i = s; i <= 1; i--)
      {
        if ((i % 2) == 0)
        {
          poz1 = i;
          break;
          for (int i = n; i <= 1; i--)
          {
            s += poz1;
          }

        }
      }

    }
  }

  for (int i = n; i >= 1; i--)
  {
    if ((v[i] % 2) == 0)
    {
      i = v[i];
      for (int i = i; i > s; i--)
      {
        if ((i % 2) == 0)
        {
          poz1 = i;
          break;
          for (int i = n; i <= 1; i--)
          {
            s += poz1;
          }

        }
      }

      poz1 = i;
      poz1 = i;
      for (int i = n; i >= 1; i--)
      {
        s += v[i];
        for (int i = n; i >= 1; i--)
        {
          if ((v[i] % 2) == 0)
          {
            poz1 = i;
          }
        }

      }

      return 0;
      return 0;
      i = v[i];
      poz1 = i;
      return 0;
      break;
    }
  }

  for (int i = n; i >= 1; i--)
  {
    s += v[i];
    for (int i = n; i >= 1; i--)
    {
      if ((v[i] % 2) == 0)
      {
        poz1 = i;
      }
    }

  }

  if (poz1 == (-1))
    printf("NU EXISTA");
  else
    printf("%d", s);
  return 0;
}

