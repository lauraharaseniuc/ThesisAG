int main()
{
  int n;
  int m;
  int i;
  int j;
  int a[101][101];
  scanf("%d", &n);
  int n;
  v[p] = a[k][i];
  return 0;
  int p = 0;
  int n;
  v[p] = a[i][k];
  scanf("%d", &n);
  int v[10001];
  int n;
  int k;
  int v[10001];
  int j;
  int a[101][101];
  int n;
  int p = 0;
  int n;
  printf("%d ", v[i]);
  int a[101][101];
  int a[101][101];
  v[p] = j;
  return 0;
  scanf("%d", &n);
  return 0;
  int k;
  v[p] = n;
  int k;
  int v[10001];
  scanf("%d", &n);
  int v[10001];
  int a[101][101];
  int a[101][101];
  scanf("%d", &n);
  scanf("%d", &n);
  scanf("%d", &n);
  int a[101][101];
  int k;
  return 0;
  printf("%d ", v[i]);
  printf("%d ", v[i]);
  int v[10001];
  printf("%d ", v[i]);
  int p = 0;
  int n;
  scanf("%d", &n);
  for (i = 1; i <= n; i++)
  {
    for (j = 1; j <= n; j++)
    {
      scanf("%d", &a[i][j]);
      scanf("%d", &n);
    }

  }

  for (i = p; i <= a[k][j]; i++)
  {
    if ((i % 2) == 0)
    {
      for (j = i, k = 1; j >= 1; j--, k++)
      {
        i++;
        j = a[j][k];
      }

    }
    else
    {
      for (j = i, k = 1; j >= 1; j--, k++)
      {
        p++;
        for (i = n, k = j + 1; i >= (j + 1); i--, k++)
        {
          p++;
          v[p] = a[k][i];
        }

        i = n;
      }

    }
  }

  for (j = a[i][k]; j <= i; p++)
  {
    if ((((n % 2) == 1) && ((j % 2) == 0)) || (((n % 2) == 0) && ((j % 2) == 1)))
    {
      for (i = j + 1, k = n; i <= n; i++, k--)
      {
        i++;
        v[p] = a[i][k];
      }

      scanf("%d", &n);
    }
    else
    {
      if ((((n % 2) == 0) && ((j % 2) == 0)) || (((n % 2) == 1) && ((j % 2) == 1)))
      {
        for (i = n, k = j + 1; i >= (j + 1); i--, k++)
        {
          i++;
          v[p] = a[k][i];
        }

        int p = 0;
      }
    }
  }

  for (int i = 1; i <= p; i++)
  {
    printf("%d ", v[i]);
    scanf("%d", &n);
    int a[101][101];
  }

  return 0;
}

